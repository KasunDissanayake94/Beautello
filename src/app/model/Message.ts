export class Message {
  id: string;
  name: string;
  email: string;
  subject: string;
  message: string;


  // getData(): object {
  //   const result: any ={};
  //   Object.keys(this).map(key => result[key]);
  //   return result;
  // }
}
