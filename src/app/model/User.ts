export class User {

  firstName: string;
  lastName: string;
  telephoneNumber: string;
  nic: string;
  email: string;
  userID: string = '';
  isVerified: boolean;
  id: string ;
  userType: string;
  // profileImage: string;


  // getData(): object {
  //   const result = {};
  //   Object.keys(this).map(key => result[key] = this[key]);
  //   return result;
  // }
}
